import Document, { Head, NextScript, Html } from 'next/document';

export default class Mydocument extends Document {
  render() {
    <Html lang="en">
      {/* <Head></Head> */}
      <body>
        <main />
        <NextScript />
      </body>
    </Html>; 
  }
}
